---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-03-15T10:00:00.000Z
provider: 
---

# 小红书开发幕后故事文案生成 Skill

## 功能概述

将产品的 **开发过程、技术决策、踩坑经历** 转化为小红书风格的幕后故事文案。通过扫描项目的代码变更、技术架构、设计决策等信息，生成 **3 个不同叙事角度** 的文案版本，让用户看到产品背后的真实故事。

这种内容在小红书上有独特的价值——用户喜欢看"怎么做出来的"，开发者的真实分享容易引发共鸣和信任。

**版本字数范围**：
- 📖 长版本：800-1200字（完整故事，深度分享）
- ⚡ 短版本：300-500字（关键片段，快速阅读）

---

## 使用方法

### 基础调用

```
@xhs-behind-the-scenes-skill 生成幕后故事文案
```

### 带参数调用

```
@xhs-behind-the-scenes-skill
主题: 简历编辑器的开发过程
风格: 真诚自然
```

或

```
@xhs-behind-the-scenes-skill
我想写一篇关于模拟面试功能从想法到上线的故事
```

---

## 支持的参数

### 1. 主题选择 (topic)
- **`功能开发`** - 某个功能从 0 到 1 的过程
- **`技术选型`** - 为什么选择了某个技术方案
- **`踩坑记录`** - 开发中遇到的问题和解决过程
- **`设计决策`** - 产品设计背后的思考
- **`数据故事`** - 用数据讲述产品成长
- **`日常开发`** - 某一天/一周的开发日常

### 2. 聚焦范围 (focus)
可选，指定具体的功能或模块
```
@xhs-behind-the-scenes-skill
主题: 功能开发
聚焦: 语音输入功能
```

### 3. 文案长度 (length)
- **`长`** (默认) - 800-1200字
- **`短`** - 300-500字

### 4. 风格选择 (style)
- **`真诚自然`** (默认) - 朋友分享故事，坦诚有温度
- **`技术向`** - 偏技术分享，对开发者友好
- **`轻松日常`** - 生活化记录，轻松好读

### 5. 情感基调 (tone)
可选，指定文案的情感走向
- **`成长感`** - 展现产品和人的成长
- **`挑战感`** - 强调克服困难的过程
- **`温暖感`** - 突出做产品的初心和热情

---

## 工作流程

### Step 1: 自动扫描项目信息 ⭐ 必要

**Skill 会自动扫描以下内容：**

| 扫描对象 | 优先级 | 提取内容 |
|---------|------|--------|
| Git 提交历史 | 🔴 最高 | 开发时间线、关键节点 |
| 代码注释 | 🟡 次高 | TODO、FIXME、HACK 等开发痕迹 |
| `README.md` | 🟡 次高 | 项目背景和动机 |
| `package.json` | 🟠 中等 | 技术栈和依赖选择 |
| `.xhs-config.json` | 🔴 最高 | 产品核心信息 |
| `src/` 代码结构 | 🟠 中等 | 架构设计和模块划分 |
| 文件修改频率 | 🟢 辅助 | 哪些模块改动最多（踩坑最多） |

**自动提取的故事素材**：
- ✅ 关键开发时间线
- ✅ 技术栈选择及理由
- ✅ 代码中的有趣注释和 TODO
- ✅ 反复修改的模块（可能是踩坑点）
- ✅ 依赖包的选型
- ✅ 项目规模（文件数、代码量、提交次数）

### Step 2: 识别潜在故事线 ⭐ 关键

基于扫描信息，Skill 自动提取 3-5 个潜在的故事线：

```
我扫描了项目信息，发现以下潜在的故事素材：

📌 故事线 1: 简历编辑器开发（修改次数最多的模块）
📌 故事线 2: 技术选型——为什么用 React + Vite
📌 故事线 3: AI 模拟面试功能的迭代过程
📌 故事线 4: PDF 导出功能的实现（代码注释暗示踩过很多坑）
📌 故事线 5: 从一个人的 Side Project 到上线

➡️ 你想写哪个故事？或者有其他想分享的？
```

### Step 3: 根据选择生成文案

每个版本有不同的叙事角度：

| 版本 | 叙事角度 | 适用场景 |
|------|--------|--------|
| **A - 时间线型** | 按时间顺序讲述开发过程 | 功能从想法到上线的完整故事 |
| **B - 问题驱动型** | 围绕一个核心问题/挑战展开 | 踩坑和解决过程，技术含量高 |
| **C - 感悟型** | 从开发过程中提炼出普适道理 | 引发共鸣，适合更广的受众 |

### Step 4: 输出成果

生成文件：
- `xhs-bts-[主题关键词]-all-versions.md`（包含全部版本文案）

---

## 文案结构规范

### 必须包含的 4 个部分

#### 1️⃣ 开头 - 钩子 (80-150字)

用一个具体的场景、问题或数据作为钩子，吸引读者继续往下看。

**要点**：
- 开头必须具体，不能抽象
- 最好有一个"转折"或"意外"
- 让读者产生好奇

**好的开头示例**：
```
花了三天写的 PDF 导出功能，结果在用户那边分页全是乱的。
改了 47 次 commit 才搞定，中间差点想放弃。
现在回头看那段代码，反而觉得它是整个项目里最扎实的部分。
```

**不好的开头**：
```
❌ 今天来聊聊我的开发过程（太笼统）
❌ 大家好，我是一个独立开发者（太平淡）
❌ 编程是一件很有意义的事情（太抽象）
```

#### 2️⃣ 主体 - 故事展开 (500-800字，取决于版本长度)

按选择的叙事角度展开故事。

**时间线型结构**：
```
[起因] → [第一次尝试] → [遇到问题] → [转变/突破] → [最终效果]
```

**问题驱动型结构**：
```
[核心问题是什么] → [试了什么方案] → [为什么不行] → [最终方案] → [学到了什么]
```

**感悟型结构**：
```
[一个具体的开发片段] → [引发的思考] → [类比到更大的道理] → [回到产品本身]
```

**要点**：
- 要有细节——具体的技术名词、数字、场景
- 要有转折——不是一帆风顺的流水账
- 要有情感——开发过程中的心情变化
- 要有收获——最终学到或解决了什么

**避免**：
- ❌ 通篇技术术语，非开发者看不懂
- ❌ 纯粹的教程或技术文档
- ❌ 没有细节的泛泛而谈
- ❌ 刻意卖惨或自夸

#### 3️⃣ 收获/感悟 (100-200字)

从故事中提炼 1-2 个具体的收获或感悟。

**要点**：
- 要具体，不要喊口号
- 和故事本身紧密相关
- 对读者有启发价值

**示例**：
```
后来我想明白了——很多时候不是代码写不好，是一开始就没想清楚用户到底怎么用。
PDF 分页的问题看起来是技术问题，本质是我没花时间去研究不同简历的真实排版。
先理解场景，再写代码。这个道理我在这次踩坑之后才真正体会到。
```

#### 4️⃣ 结尾 - 互动引导 (60-100字)

**要点**：
- 把话题抛给读者
- 邀请有类似经历的人分享
- 简洁、温暖

**示例**：
```
你在开发中有没有遇到过类似的"看起来简单，做起来要命"的功能？
评论区聊聊，说不定还能帮到彼此 ～
```

---

## 版本详细说明

### 长版本 (800-1200字)

**适用场景**：
- 讲述一个完整的开发故事
- 有很多有趣的细节可以分享
- 想建立深度的信任和共鸣

**特点**：
- ✅ 完整的故事弧线（起因→过程→结果）
- ✅ 丰富的细节和场景描写
- ✅ 穿插情感和思考
- ✅ 结尾有深度的感悟

### 短版本 (300-500字)

**适用场景**：
- 分享一个简短的开发片段
- 快速记录一个踩坑点
- 日常开发记录

**特点**：
- ✅ 聚焦一个点，不铺开
- ✅ 快速切入，快速收尾
- ✅ 有一个明确的"takeaway"

---

## 风格差异

### 真诚自然 (默认)

```
语气特点：像和朋友聊天，分享真实的开发经历
细节：穿插真实的情感（焦虑、兴奋、沮丧、成就感）
技术内容：用大白话解释，非技术用户也能看懂
整体感觉：温暖、真实、有代入感
```

### 技术向

```
语气特点：偏向技术分享，但仍然有可读性
细节：包含更多技术决策、代码片段、架构思考
技术内容：可以深入一些，但避免变成文档
整体感觉：专业、有料、适合开发者群体
```

### 轻松日常

```
语气特点：轻松幽默的日常记录风
细节：用调侃的语气讲踩坑、用生活化的比喻解释技术
技术内容：尽量用比喻和类比，降低门槛
整体感觉：有趣、好读、能会心一笑
```

---

## 参数解析规则

| 用户输入 | 识别为 |
|---------|------|
| 「讲讲怎么做的」「开发过程」 | topic: 功能开发 |
| 「为什么选...」「技术选型」 | topic: 技术选型 |
| 「踩坑」「遇到问题」「Bug 故事」 | topic: 踩坑记录 |
| 「为什么这样设计」「设计思路」 | topic: 设计决策 |
| 「增长」「数据变化」「用户量」 | topic: 数据故事 |
| 「今天做了什么」「日常」 | topic: 日常开发 |

---

## 输出示例

### 示例：PDF 导出踩坑记录 版本 B（问题驱动型，约 900 字）

```markdown
# 小红书幕后故事文案 - PDF 导出踩坑 版本 B（长）

花了三天写的简历 PDF 导出功能，上线第一天就收到反馈："我的简历导出来分页全乱了，内容被切到两页中间。"

当时心态直接崩了。因为在我自己的测试环境里，一切都是正常的。

---

## 问题出在哪

简历排版看着简单，但每个人的简历长度完全不一样。有人一页就能写完，有人需要两三页。关键是——内容不能从一个段落中间被切断。

比如"项目经历"这一块，不能上半段在第一页、下半段在第二页。用户会觉得简历"碎了"。

我最初的方案很天真：按固定高度切分页面。1123px 一页，超过就换页。

结果发现——用户的字体大小、行间距、项目段落长度千差万别。固定高度切分，大概率切到某一行文字的中间。

## 试了什么方案

第一版：用 CSS 的 `page-break-inside: avoid` 属性。理论上浏览器会自动避免在元素中间分页。实际效果——完全不可控。不同浏览器的表现还不一样，Chrome 和 Safari 分页位置差了几十像素。

第二版：手动计算每个 DOM 元素的高度，累加到一页的高度时强制插入分页符。好了一些，但遇到嵌套元素（比如项目经历里包含多个小段落）就又乱了。

第三版，也就是最终方案：逐像素扫描。用 canvas 把简历渲染出来，然后从上往下一行一行扫描像素颜色。找到"连续空白行"的位置作为分页点。

听起来很笨对吧？但它是唯一一个不依赖 DOM 计算、不受 CSS 排版差异影响的方案。

## 47 次 Commit

从第一版到最终方案，Git 记录显示我改了 47 次 commit。中间有好几次想过"要不算了，直接让用户自己截图拼 PDF"。

最后让我坚持下来的是——有个用户在反馈里说"如果导出能好用一点，我就不用每次都手动调 Word 了"。

一个很小的需求，但想到有人真的在等这个功能，就不好意思放弃了。

## 最终效果

现在的 PDF 导出，不管简历多长、排版多复杂，分页都能切在段落之间的空白处。还加了分页预览——在编辑器里就能看到虚线标记，知道哪里会分页。

用了两周，没有再收到"分页乱"的反馈。

---

后来我想明白了一件事：开发中最难的往往不是技术复杂的功能，而是那些"看起来很简单"的细节。

PDF 导出听起来就是一个"导出"按钮的事，但背后的排版兼容问题足够让你怀疑人生。

做产品就是这样——用户觉得"自然而然"的体验，往往是开发者踩了无数坑之后才做到的。

---

你在做项目的时候有没有遇到过类似的情况？看起来一天能搞定，结果花了一周？
评论区聊聊，大家一起吐槽 ～

🔗 offerin.co
```

---

## 使用建议

1. **定期分享** - 每上线一个重要功能都可以写一篇幕后故事
2. **关注踩坑点** - 踩坑故事比成功故事更有吸引力和共鸣
3. **保持真实** - 不需要美化过程，真实的挣扎更打动人
4. **适度技术** - 技术细节要有，但用大白话讲，非开发者也能看懂
5. **和 product-intro 搭配** - product-intro 讲产品是什么，behind-the-scenes 讲产品怎么来的

**理想工作流**：
```
@xhs-behind-the-scenes-skill 
主题=踩坑记录 
聚焦=PDF导出功能
风格=真诚自然
  → Skill 自动扫描相关代码和 Git 历史
  → 提取故事素材和关键节点
  → 用户确认故事方向
  → 生成 3 个版本文案
  → 用户选择最适合的版本发布
```

---

## 常见问题

**Q: 我不是技术型产品，也能用吗？**
A: 当然。所有产品都有开发过程和幕后故事。非技术型产品可以聊设计决策、用户调研、需求取舍等。

**Q: 会不会暴露代码细节？**
A: 不会。Skill 只提取故事素材（时间线、决策、问题），不会在文案中包含具体代码。除非你选择了"技术向"风格且明确要求。

**Q: 故事素材不够丰富怎么办？**
A: Skill 会在 Step 2 列出找到的素材，如果不够，你可以补充自己的经历和想法。最好的故事往往来自你的记忆而不是代码。

**Q: 这种内容在小红书有人看吗？**
A: 开发者日常、产品幕后在小红书越来越受欢迎。关键是讲得真实、有细节，让人有代入感。

---

**准备好分享你的开发故事了吗？**
```
@xhs-behind-the-scenes-skill 生成幕后故事文案
```
