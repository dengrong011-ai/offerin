---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-03-15T10:00:00.000Z
provider: 
---

# 小红书更新日志文案生成 Skill

## 功能概述

将产品的 **版本更新记录** 自动转化为小红书风格的更新日志文案。通过扫描项目的 Git 提交记录、CHANGELOG 文件、Release Notes 等信息，生成 **3 个不同切入角度** 的文案版本，让每次更新都能成为一次有效的用户沟通。

**版本字数范围**：
- 📖 长版本：600-900字（详细更新说明 + 开发心路）
- ⚡ 短版本：200-400字（核心亮点速览）

---

## 使用方法

### 基础调用

```
@xhs-changelog-skill 生成更新日志文案
```

### 带参数调用

```
@xhs-changelog-skill 
版本: v2.1.0
风格: 真诚自然
重点: 模拟面试功能升级
```

或

```
@xhs-changelog-skill
我需要短版本，重点说简历优化新增的模板功能
```

---

## 支持的参数

### 1. 版本号 (version)
- 指定需要生成文案的版本号
- 留空则自动获取最新版本的更新内容

### 2. 时间范围 (range)
- **`最近一次`** (默认) - 最近一个版本的更新
- **`最近一周`** - 过去 7 天的所有变更
- **`最近一月`** - 过去 30 天的所有变更
- **`自定义`** - 指定起止版本号或日期

### 3. 文案长度 (length)
- **`长`** (默认) - 600-900字，包含更新详情和开发想法
- **`短`** - 200-400字，核心更新速览

### 4. 风格选择 (style)
- **`真诚自然`** (默认) - 开发者和用户聊天的口吻
- **`专业清晰`** - 突出功能价值和改进逻辑
- **`轻松亲切`** - 有趣的更新播报、接地气的比喻

### 5. 重点功能 (focus)
可选，指定某个更新详细展开

---

## 工作流程

### Step 1: 自动扫描更新信息 ⭐ 必要

**Skill 会自动扫描以下内容：**

| 扫描对象 | 优先级 | 提取内容 |
|---------|------|--------|
| `CHANGELOG.md` | 🔴 最高 | 版本更新记录 |
| `release-notes/` 目录 | 🟡 次高 | 版本发布说明 |
| Git 提交记录 | 🟡 次高 | 最近的 commit messages |
| `package.json` version | 🟠 中等 | 当前版本号 |
| GitHub/GitLab Releases | 🟠 中等 | 发布页面信息 |
| 新增/修改的文件列表 | 🟢 辅助 | 推断变更范围 |

**自动提取的信息维度**：
- ✅ 版本号和发布日期
- ✅ 新增功能列表
- ✅ 优化改进项
- ✅ Bug 修复列表
- ✅ 重大变更 (Breaking Changes)
- ✅ 影响的用户场景

### Step 2: 分类整理更新内容 ⭐ 关键

将扫描到的更新按用户感知度分类：

| 类别 | 说明 | 文案权重 |
|------|------|---------|
| 🎉 **亮点功能** | 全新功能，用户期待的 | ⭐⭐⭐ 重点展开 |
| ⚡ **体验优化** | 速度提升、交互改进 | ⭐⭐ 适度说明 |
| 🐛 **问题修复** | 用户反馈的 Bug | ⭐ 简要提及 |
| 🔧 **技术升级** | 底层优化，用户无感 | ☆ 一笔带过或不提 |

### Step 3: 与用户确认范围

```
我扫描到以下更新内容：

【版本】v2.1.0（2026-03-15）
【新功能】简历模板选择（3 种新模板）
【优化】面试评估报告增加雷达图
【修复】语音输入偶尔中断的问题
【技术】数据库查询性能优化

➡️ 确认：你要为这次更新生成文案吗？
要重点突出哪个更新？
```

### Step 4: 根据确认生成文案

每个版本有不同的切入角度：

| 版本 | 切入角度 | 适用场景 |
|------|--------|--------|
| **A - 功能亮点型** | 直接展示新功能价值 | 有重大新功能上线时 |
| **B - 用户驱动型** | 从用户反馈引出更新 | 更新主要来自用户需求 |
| **C - 迭代故事型** | 讲述开发过程和思考 | 建立长期信任和共情 |

### Step 5: 输出成果

生成文件：
- `xhs-changelog-[版本号]-all-versions.md`（包含全部版本文案）

---

## 文案结构规范

### 必须包含的 4 个部分

#### 1️⃣ 版本标题 (一句话)

用一句话概括这次更新的核心价值，作为小红书标题的备选。

**示例**：
```
Offerin v2.1 来了：简历终于有模板可以选了 📝
```

#### 2️⃣ 开头 - 更新背景 (60-100字)

交代为什么做这次更新，用户痛点或上线原因。

**要点**：
- 从用户反馈或使用场景切入
- 自然过渡到"所以我们更新了..."
- 真实、不做作

**示例**：
```
上次发布后收到好多反馈说"简历能不能好看一点"，确实之前只有一种模板有点单调。
这周花了不少时间重新设计了模板系统，终于可以选了。
```

#### 3️⃣ 主体 - 更新详情 (300-600字，取决于版本长度)

逐个介绍重要更新，按用户感知度排序。

**每个更新段落结构**：
```
[更新名称] ✨

[更新解决什么问题]。
[具体怎么用 / 前后对比]。
[一句话感受或数据]。
```

**要点**：
- 优先讲用户能感知到的变化
- 配合使用场景，说清楚"什么时候会用到"
- 有 before/after 对比更好
- Bug 修复不需要长篇大论，一句话即可

**避免**：
- ❌ 罗列技术参数（如"减少了 30ms 延迟"）
- ❌ 写成技术文档式的 changelog
- ❌ 流水账列举每个小修改

#### 4️⃣ 结尾 - 预告与互动 (80-120字)

说明接下来的计划，引导互动。

**要点**：
- 简短预告下一步要做什么
- 欢迎反馈，引导用户说出需求
- 温暖、有对话感

**示例**：
```
下一步准备做面试记录的导出功能，如果你有其他想要的功能，评论告诉我。
每次更新都会发一篇记录，关注不迷路 ～
```

---

## 版本详细说明

### 长版本 (600-900字)

**适用场景**：
- 有重大新功能上线
- 需要解释更新背景和思考
- 建立和用户的长期信任

**特点**：
- ✅ 每个重要更新展开说明（配使用场景和 before/after）
- ✅ 融入开发思考过程
- ✅ 结尾有明确的下一步预告

### 短版本 (200-400字)

**适用场景**：
- 常规优化和 Bug 修复
- 快速通知用户
- 评论区补充信息

**特点**：
- ✅ 直接列出核心更新
- ✅ 每个更新 1-2 句话说清
- ✅ 结尾简短有力

---

## 风格差异

### 真诚自然 (默认)

```
语气特点：开发者和用户聊天，分享更新的原因和思考
开头：「上次收到反馈说...」「最近发现一个问题...」
更新介绍：「所以我做了...」「试了好几个方案，最后选了...」
结尾：「下一步想做...，你们觉得呢？」
```

### 专业清晰

```
语气特点：结构化，突出功能价值
开头：「v2.1.0 正式发布，本次更新聚焦于...」
更新介绍：按类别分组，功能→优化→修复
结尾：「更多功能规划请关注后续更新」
```

### 轻松亲切

```
语气特点：有趣、轻松、像朋友播报新闻
开头：「又来汇报工作了～」「猜猜这次更新了什么？」
更新介绍：用生活化比喻，接地气的表达
结尾：「催更请扣 1，提需求请扣 2 ～」
```

---

## 参数解析规则

| 用户输入 | 识别为 |
|---------|------|
| 「v2.1」「版本 2.1」「最新版」 | version: [具体版本号] |
| 「这周的更新」「最近」 | range: 最近一周 |
| 「详细版」「完整版」 | length: 长 |
| 「简短版」「速览版」 | length: 短 |
| 「突出...」「重点说...」 | focus: [功能名] |

---

## 输出示例

### 示例：Offerin v2.1.0 长版本 - 版本 A（约 700 字）

```markdown
# 小红书更新日志文案 - Offerin v2.1.0 版本 A（长）

Offerin 又更新了 📝 这次终于可以选简历模板了

---

上次发帖后好多人问：简历能不能好看一点？之前确实只有一种排版，不管什么行业都一个样子。

这周花了不少时间重新设计了模板系统，终于上线了。一起更新的还有面试评估的可视化和一个语音输入的 Bug 修复。

## 简历模板选择 ✨

现在有 3 种模板可以选了：

经典模板——适合大部分行业，信息密度高，排版工整。
简约模板——留白多一些，适合设计、市场类岗位。
专业模板——分栏设计，适合技术岗和有多段经历的简历。

切换模板的时候内容会自动适配，不用重新排版。选好模板后可以实时预览，左边编辑右边看效果。

之前有用户说"简历内容不错但排版太朴素了，HR 可能第一眼就划走了"，希望模板能帮到这一点。

## 面试评估雷达图 ✨

面试结束后的评估报告，之前只有文字描述。现在加了雷达图——沟通表达、专业能力、逻辑思维、学习能力等维度一目了然。

一眼就能看出自己哪个维度强、哪个维度需要补。比读一大段文字直观多了。

## 修复：语音输入偶尔中断 🐛

之前有用户反馈在面试模式下语音输入有时会莫名中断。排查了一下是音频流处理的一个边界条件没覆盖到。这版修掉了，语音输入应该稳定了。

如果还遇到问题，随时告诉我。

---

下一步准备做面试记录的对比功能——可以把多次面试的评估放在一起看进步趋势。另外简历模板也会继续加新的。

有想要的功能或者发现的问题，评论区告诉我 ～

🔗 offerin.co
```

---

## 使用建议

1. **每次版本更新后调用** - 养成习惯，更新记录也是内容积累
2. **重大更新用长版本** - 新功能值得好好讲
3. **日常修复用短版本** - 告知用户在维护就行
4. **选择用户驱动型(B版)** - 如果更新是因为用户反馈，强调这一点用户会很有参与感
5. **保持更新节奏** - 定期发更新日志，让用户感受到产品在进步

**理想工作流**：
```
@xhs-changelog-skill 版本=v2.1.0 长版本
  → Skill 自动扫描 CHANGELOG 和 Git 记录
  → 列出更新内容并确认重点
  → 用户确认聚焦范围
  → 生成 3 个版本文案
  → 用户选择最合适的版本发布
```

---

## 常见问题

**Q: 没有 CHANGELOG 文件怎么办？**
A: Skill 会自动回退到扫描 Git 提交记录和代码变更。有 CHANGELOG 效果最好，建议维护。

**Q: 技术更新需要写进文案吗？**
A: 用户无感的技术更新（如性能优化、代码重构）一般不写。除非优化效果用户能直接感知（如"页面加载快了一倍"）。

**Q: 更新内容太少怎么办？**
A: 可以把多次小更新合并成一篇，用 `range=最近一月` 参数。

**Q: 可以和 product-intro 一起用吗？**
A: 当然。product-intro 用于首次介绍产品，changelog 用于后续持续更新。

---

**准备好了吗？**
```
@xhs-changelog-skill 生成更新日志文案
```
