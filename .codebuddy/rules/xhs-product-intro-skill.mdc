---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-03-14T19:54:50.551Z
provider: 
---

# 小红书产品介绍文案生成 Skill

## 功能概述

为任何开发完成的产品自动生成小红书产品介绍文案。通过智能扫描项目信息，生成 **3 个不同切入角度** 的文案版本，支持长/短两种版本、多种风格参数定制。

**版本字数范围**：
- 📖 长版本：800-1000字（完整介绍，一次发布）
- ⚡ 短版本：200-400字（快速引导，评论区使用）

---

## 使用方法

### 基础调用

```
@xhs-product-intro-skill 生成小红书文案
```

### 带参数调用

```
@xhs-product-intro-skill 
版本: 长  
风格: 真诚自然
```

或

```
@xhs-product-intro-skill
我需要短版本的文案，风格要轻松一点，突出简历优化功能
```

---

## 支持的参数

### 1. 版本选择 (version)
- **`长`** (默认) - 800-1200字，功能深度介绍，建立信任
- **`短`** - 500-700字，快速引导，鼓励互动

### 2. 风格选择 (style)  
- **`真诚自然`** (默认) - 朋友聊天感，分享真实想法
- **`专业清晰`** - 严谨专业，强调功能价值
- **`轻松亲切`** - 轻松幽默，贴近用户心理

### 3. 重点功能 (focus)
可选，指定某个功能详细展开介绍
```
@xhs-product-intro-skill 
版本: 长
重点功能: 简历优化
```

### 4. 目标用户 (target_user)
可选，补充或覆盖扫描到的目标用户群
```
@xhs-product-intro-skill
目标用户: 应届毕业生、求职转行者
```

---

## 工作流程（优化版）

### Step 1: 自动扫描并提取信息 ⭐ 必要

**Skill 会自动扫描以下内容（用户不需要做任何事）：**

| 扫描对象 | 优先级 | 提取内容 |
|---------|------|--------|
| `.xhs-config.json` | 🔴 最高 | 产品名称、描述、核心功能、目标用户等 |
| `package.json` | 🟡 次高 | 产品名称、描述、版本 |
| `README.md` | 🟡 次高 | 产品概述、功能列表、使用说明 |
| `docs/` 目录 | 🟠 中等 | 详细功能文档、特性说明 |
| `src/` 结构 | 🟠 中等 | 关键模块名称、功能推断 |
| 所有代码文件中的注释 | 🟠 中等 | 隐藏的功能描述和设计意图 |

**自动提取的信息维度**：
- ✅ 产品名称和定位
- ✅ 核心功能列表（按重要度排序）
- ✅ 目标用户群体
- ✅ 产品解决的主要痛点（2-3 个）
- ✅ 差异化特点
- ✅ 使用场景
- ✅ 技术栈和框架（可选）

### Step 2: 与用户确认范围 ⭐ 关键

**在生成文案前，Skill 必须与用户确认**：

```
我已扫描到以下信息：

【产品】Offerin - 一站式求职工具
【功能】简历优化、AI模拟面试、资料管理
【用户】应届毕业生、求职转行者
【用户输入】只要职业探索功能

➡️ 确认：你要生成的是「职业探索功能」的文案，对吗？
还是说整个产品的介绍？
```

**用户确认后的处理**：
- ✅ 确认单个功能 → 聚焦该功能详细展开
- ✅ 确认整个产品 → 生成完整产品文案
- ✅ 需要补充信息 → 请用户提供缺失信息

### Step 3: 根据确认生成文案

每个版本都有不同的切入角度和侧重点：

| 版本 | 切入角度 | 适用场景 |
|------|--------|--------|
| **A - 功能深度型** | 痛点 → 功能详解 → 使用流程 | 需要深度说明，建立专业感 |
| **B - 用户心理型** | 用户感受 → 体验优化 → 反馈欢迎 | 强调易用性和温暖感 |
| **C - 对比创新型** | 问题对比 → 创新解决 → 差异化 | 突出竞争优势和创新点 |

### Step 4: 输出成果

生成文件：
- `xhs-intro-[产品/功能]-version-A-长.md` (版本A-长)
- `xhs-intro-[产品/功能]-version-B-长.md` (版本B-长)
- `xhs-intro-[产品/功能]-version-C-长.md` (版本C-长)
- `xhs-intro-[产品/功能]-version-A-短.md` (版本A-短，可选)
- `xhs-intro-[产品/功能]-version-B-短.md` (版本B-短，可选)
- `xhs-intro-[产品/功能]-version-C-短.md` (版本C-短，可选)

---

## 文案结构规范

### 必须包含的 3 个部分

#### 1️⃣ 开头 - 痛点场景 (80-120字)

用 2-3 个真实的用户痛点切入，让目标用户「被戳中」。

**要点**：
- 每个痛点用一句话，以问句或陈述句形式
- 语气真实、共鸣，不卖课、不夸张
- 必须是用户的**真实困境**，不是虚构问题

**示例**：
```
简历怎么写才能突出亮点，又不显得浮夸？
面试前总觉得准备不足，临时抱佛脚也没用。
投的简历如何匹配岗位要求，才能真的提升面试邀请率？
```

#### 2️⃣ 主体 - 产品介绍 (400-700字，取决于版本长度)

从痛点自然过渡到产品，逐个介绍核心功能。

**要点**：
- **功能→痛点对应**：每个功能都解决一个上面提到的痛点
- **精简介绍**：核心功能优先介绍，非核心功能可略过或轻带
- **配合使用场景**：简洁说明在什么情况下用这个功能
- **语言风格**：像朋友聊天，每句话都有信息量，避免冗长

**每个功能段落结构**：
```
[功能名字]

[简洁描述功能解决什么问题]，所以我做了这个功能。

[功能怎么用 / 使用流程]。

[用这个功能的感受 / 数据 / 用户反馈]。
```

**避免**：
- ❌ 「我们这个产品」→ ✅ 「这个工具」或「它」
- ❌ 「非常」「特别」「完全」等修饰词
- ❌ 「100% 成功」「绝对有效」等过度承诺
- ❌ 技术术语和复杂逻辑

#### 3️⃣ 结尾 - 互动引导 (80-120字)

真诚表达产品现状，欢迎反馈，引导互动。

**要点**：
- 坦白说产品还在迭代或完善中
- 表达欢迎反馈的真诚态度
- 2-3 句简洁引导（关注/反馈/使用）
- 温暖、不强硬

**示例**：
```
还在继续打磨中，特别想听听你的反馈。
有任何建议或问题，评论或私信我都可以～
如果觉得有帮助，欢迎关注或分享给身边找工作的朋友。
```

---

## 版本详细说明

### 长版本 (800-1000字)

**适用场景**：
- 首次发布产品介绍
- 需要深度建立信任
- 小红书单篇发布（一次性呈现完整产品）

**特点**：
- ✅ 功能核心展开（2-3 个主要功能详细说明）
- ✅ 配合使用场景
- ✅ 可以加入用户反馈或简短数据
- ✅ 结尾欢迎反馈

### 短版本 (200-400字)

**适用场景**：
- 快速引导、评论区补充
- 功能聚焦、快速切入价值
- 二次传播、信息精简

**特点**：
- ✅ 突出 1-2 个核心功能
- ✅ 快速切入痛点和解决方案
- ✅ 结尾简短有力，鼓励互动

---

## 风格差异

### 真诚自然 (默认)

```
语气特点：朋友聊天感、坦诚分享、有温度
开头：用亲身经历引入
功能介绍：「我做了个功能...」而不是「产品提供...」
结尾：「特别想听听你的想法」而不是「欢迎反馈」
```

### 专业清晰

```
语气特点：严谨专业、逻辑清晰、强调价值
开头：用数据或现象引入
功能介绍：「该功能通过...实现...」，强调原理
结尾：「我们将持续优化...」，展现专业态度
```

### 轻松亲切

```
语气特点：轻松幽默、亲近感强、容易记住
开头：用对话或场景感引入
功能介绍：举生活化的例子
结尾：鼓励互动，可加入轻松语气词
```

---

## 参数解析规则

Skill 会自动识别以下表述方式：

| 用户输入 | 识别为 |
|---------|------|
| 「长版本」「详细版」「完整版」 | version: 长 |
| 「短版本」「简短版」「精简版」 | version: 短 |
| 「真诚」「自然」「朋友感」 | style: 真诚自然 |
| 「专业」「清晰」「严肃」 | style: 专业清晰 |
| 「轻松」「幽默」「亲切」 | style: 轻松亲切 |
| 「突出...」「重点...」「详细...」 | focus: [功能名] |
| 「我的用户是...」「针对...」 | target_user: [用户描述] |

---

## 输出示例

### 示例：Offerin 长版本 - 版本 A（约 900 字）

```markdown
# 小红书产品介绍文案 - Offerin 版本 A（长）

简历怎么写才能突出亮点，又不显得浮夸？  
面试前总觉得准备不足，却又不知道从何入手。  
投出去的简历为什么总是石沉大海？

最近我在打磨一个求职工具 Offerin，就是想解决这些问题。

## 简历优化

简历其实就是你的第一张面孔。但很多人不知道怎么写——要么内容太啰嗦，要么关键信息不够突出。

所以我做了简历诊断功能。你上传目标 JD 和初版简历，模型会直接告诉你和目标岗位的差距在哪。不是笼统的「加强技能」，而是具体到「这个项目经历可以这样表述会更有竞争力」。

生成新简历后，左边编辑右边预览，可以自己改，也可以选中某段让模型帮微调。用户反馈说，优化一轮下来简历质感提升明显，后续收到的面试邀请也增多了。

## 模拟面试

优化完简历，接下来就是真正的准备。我加了 AI 模拟面试功能。可以根据岗位、行业选择不同的面试场景，AI 会像真实面试官一样提问。每次练习系统都会记录下来，帮你逐步改进。

每次模拟结束，系统输出评估报告——哪些回答表现不错，哪些需要改进。还有反问清单，因为面试不是单向的，问对问题也很重要。

## 资料管理

求职是个过程，你可能同时准备多家公司的面试，简历也在不断迭代。所以我做了储存库功能——每一版简历、每一次面试记录都能保存，随时回看之前的想法，不用每次都从零开始。

---

目前还在持续打磨中，特别想听听你的想法。如果有帮助，欢迎关注或分享给身边正在找工作的朋友 ～
```

### 示例：Offerin 短版本 - 版本 A（约 300 字）

```markdown
# 小红书产品介绍文案 - Offerin 版本 A（短）

简历怎么写才能吸引面试官？  
面试前怎样才能有针对性地准备？

最近我做了一个求职工具 Offerin，想解决这些问题。

## 简历诊断

你上传 JD 和简历，AI 会告诉你具体差在哪，并直接生成优化版本。左边编辑右边预览，可以自己改，也可以让 AI 帮你微调。用户反馈说这一轮优化下来，面试邀请率真的提升了。

## 模拟面试

根据目标岗位进行 AI 模拟面试，可以一遍遍练习。每次模拟后系统给出评估报告和反问清单，帮你更系统地准备。

---

还在继续完善中，特别想听你的反馈～
```

---

## 使用建议

1. **让 Skill 自动扫描** - 直接调用，无需详细说明，Skill 会读取所有文档和代码
2. **仔细确认范围** - Skill 会列出扫描到的信息，你确认是要单个功能还是整个产品
3. **指定参数优化** - 确认后可继续指定风格、长度、额外重点等细节
4. **对比多版本** - 生成 3 个版本后，选择最符合风格的
5. **反复编辑迭代** - 生成的 `.md` 文件完全可修改，根据反馈继续优化

**理想工作流**：
```
@xhs-product-intro-skill 长版本 只写职业探索功能
  → Skill 自动扫描全部文档
  → 列出扫描结果并确认用户意图
  → 用户确认「职业探索功能」单独介绍
  → 生成 3 个版本
  → 用户选择最喜欢的版本发布
```

---

## 技术说明

### Skill 执行流程（优化版）

```
用户调用 @xhs-product-intro-skill [参数]
          ↓
🔍 解析参数 (version, style, focus, target_user)
          ↓
📂 自动扫描项目（优先级顺序）
  1️⃣ .xhs-config.json
  2️⃣ package.json + README.md
  3️⃣ docs/ 目录和代码文件
  4️⃣ src/ 结构推断
          ↓
🎯 构建产品信息 Profile
  - 产品名、功能列表、用户、痛点、差异化
          ↓
❓ 确认用户意图（关键步骤）
  "我扫描到 [产品 A] 有这些功能...
   你要的是 [功能 B] 的文案，还是整个产品？"
          ↓
✅ 用户确认后 → 聚焦范围
          ↓
📝 为确认后的范围生成 3 个版本 × 2 个长度
  - 版本 A (功能深度型)
  - 版本 B (用户心理型)
  - 版本 C (对比创新型)
  - 各版本有长/短两个版本
          ↓
💾 保存为 .md 文件
  格式：xhs-intro-[产品/功能]-version-[A/B/C]-[长/短].md
          ↓
✨ 返回生成摘要和文件位置
```

### 如何扫描产品信息

1. **自动提取**：从项目配置文件和文档中提取
2. **智能推断**：基于代码结构和命名规范推断功能
3. **用户补充**：允许用户在参数中补充或覆盖

---

## 常见问题

**Q: Skill 每次都要自动扫描吗？**  
A: 是的。自动扫描是新工作流的核心，这样能确保：
   - 捕捉最新的功能信息（代码更新后自动同步）
   - 发现隐藏的功能或痛点（通过代码注释）
   - 避免遗漏重要信息

**Q: 如何快速确认用户意图？**  
A: Skill 会自动列出扫描结果，然后问：「你要的是功能 A、功能 B、还是整个产品？」
   你直接回答即可，不需要重复描述。

**Q: 生成的文案质量如何?**  
A: 取决于项目文档完整度。建议：
   - ✅ 在项目根目录创建 `.xhs-config.json` 详细配置
   - ✅ 写清楚 README.md 中的功能描述
   - ✅ 为重要功能补充 docs/ 文档
   这样生成质量会明显提升。

**Q: 可以修改生成的文案吗?**  
A: 完全可以。生成的 `.md` 文件是最终稿，可随意编辑和调整。

**Q: 支持修改参数重新生成吗?**  
A: 支持。重新调用 Skill 会覆盖之前生成的文件。
   示例：`@xhs-product-intro-skill 短版本 风格=轻松 重点=简历优化`

**Q: 如果确认错了怎么办？**  
A: 重新调用 Skill，它会重新扫描并要求确认。无需担心，可以反复调整。

**Q: 可以用于其他平台吗?**  
A: 目前针对小红书优化。如需其他平台版本，调用时指定：
   `@xhs-product-intro-skill 平台=抖音` 或 `@xhs-product-intro-skill 平台=B站`

---

## 推荐的 .xhs-config.json 模板

在项目根目录创建此文件，Skill 会自动读取：

```json
{
  "productName": "Offerin",
  "tagline": "让求职更有策略",
  "description": "一站式求职工具，帮助求职者优化简历、模拟面试、管理资料",
  "targetUsers": "正在找工作的大学生、应届毕业生、职场转行者",
  "coreFeatures": [
    "简历优化与诊断",
    "AI 模拟面试",
    "资料管理与沉淀"
  ],
  "problems": [
    "不知道如何撰写有竞争力的简历",
    "缺乏系统的面试准备",
    "求职过程信息混乱，难以管理"
  ],
  "differentiation": "AI 驱动的个性化诊断，真实面试官级别的模拟体验",
  "style": "真诚、实用、不卖课",
  "platforms": ["小红书", "抖音"],
  "focusPoints": ["简历优化", "模拟面试"]
}
```

---

**准备好开始生成文案了吗？** 
```
@xhs-product-intro-skill 生成小红书文案 版本=长 风格=真诚自然
```