# 全局 API 成本与定价讨论稿

> 目的：从**代码中的用量与价格**出发，集合**可预期的其它成本**，讨论当前定价是否合理，并给出**可执行的定价逻辑与改动边界**。  
> 说明：下文 **「实证测算」** 已填入你提供的截图数据；**原则性章节**仍保留。汇率、币种若与控制台不一致，以导出账单为准。

---

## 〇、实证测算（基于你提供的两张仪表盘）

### 0.1 数据来源与口径

| 来源 | 内容 | 注意 |
|------|------|------|
| 费用面板 | 区间 **2026-03-15～03-21（7 天）**，**总费用 $58.09**（控制台写「费用」，与「节省」相加为总费用） | 币种以控制台为准（常见为 **USD**）；顶部 **月限额 SGD 1,000、当月已用 SGD 76.72** 可能为 **另一币种/另一计费视图**，合并解读前先对齐导出 CSV。 |
| 请求面板 | **2026-03-16～03-23（8 天）**，约 **5,100～5,200 次** API 请求（柱状图估读） | 与上表 **7 天窗口不完全重合**；下列「单次请求成本」为 **粗算**。 |

### 0.2 由 7 天费用外推的月化 API 成本

取 **7 天总费用 58.09**（记为 \(F_7\)，单位与控制台一致）：

\[
\text{日均} = \frac{58.09}{7} \approx \mathbf{8.30}
\]

\[
\text{月化（30 天线性外推）} = 58.09 \times \frac{30}{7} \approx \mathbf{249.0}
\]

即：在 **与这 7 天流量同分布** 的前提下，**仅该项 API 费用** 约 **249 / 月**（与控制台币种一致；若为 **USD**，则量级为 **~$249/月**）。

**与月累计 SGD 76.72 的关系**：若 76.72 为 **整个 3 月截至截图日**、而 58.09 为 **另一币种的一周切片**，二者不能直接加减；**运营上请以「同币种、同项目」的月度导出为准**。

### 0.3 由请求量粗算的「单次请求摊销成本」

取 **8 天总请求约 5,200**（与文中「约 5,100」取整一致），仅作量级：

\[
\frac{58.09}{5200} \approx \mathbf{0.0112}\ \text{（每请求，与 }F_7\text{ 同币种）}
\]

**若用 3/16～3/22 七天请求之和近似**（略去 3/23）：\(150+250+550+800+2950+350+50 = 5050\)，则：

\[
\frac{58.09}{5050} \approx \mathbf{0.0115}/\text{请求}
\]

**重要限制**：Gemini **按 token 计费**，不是按 HTTP 次数；不同请求价差可达数量级。该数字仅表示 **「这周总费用 ÷ 总次数」的混合均价**，用于感知量级，**不能**用来预测单次诊断/单场面试的真实成本。

### 0.4 3/20 峰值对费用与成功率的影响（定性 + 量级）

根据请求图：

- **3/20** 单日约 **2,950 次** 请求，占 8 天总量 \(\approx 2950/5200 \approx \mathbf{57\%}\)。
- 同日 **成功率约 35%**，为全程最低。

含义：

1. **成本**：若失败请求仍产生 **已发 token**（常见：流式中断、重试、部分输出），则 **低成功率日** 可能出现 **「钱花了、有效完成少」**；若上游对失败不计费，则费用主要来自 **成功部分 + 重试**。需从账单看 **3/20 当日费用** 是否同比例尖峰（你第二张图里 **费用峰值在 3/19**，与请求峰值 **3/20** 差一天，可能为 **UTC 边界** 或 **计费延迟**，建议按 **小时级导出** 对齐）。
2. **定价**：该形态说明成本受 **偶发洪峰 / 429** 影响大；**限流、退避、默认用 Flash、失败不计次**（你们 `REVIEW_ACTION_ITEMS` 里已有方向）会直接影响毛利。

### 0.5 与当前定价（¥）对照：保本用户数（仅 API、极简假设）

以下仅将 **月化 API 成本** 与 **VIP 标价** 对比，**忽略** Vercel/Supabase/人工/税，并假设 **全部 API 成本由 VIP 收入覆盖**（保守上界：实际还有免费用户与单次购买）。

取 **月化费用 ≈ 249**（与 \(F_7\) 同币种）。  
取汇率 **1 USD = 7.2 CNY**（仅示例，请按实时的付汇汇率替换）：

\[
249\ \text{USD} \times 7.2 \approx \mathbf{1793\ \text{CNY/月}}\ \text{（API 外推成本）}
\]

**月度 VIP ¥29.9**，若 **毛收入** 近似取 29.9（先 **不计** 支付手续费）：

\[
\frac{1793}{29.9} \approx \mathbf{60}\ \text{个付费 VIP/月}
\]

即：在 **当前这周流量水平** 下，若 **全部 API 成本** 都要靠 **¥29.9 月费** 覆盖，大约需要 **~60 个** 持续付费月度会员 **仅抵 API 一项**（真实还需加其它成本，且免费用户也会吃 token，故 **实际所需付费用户数更高**）。

**冲刺 ¥19.9 / 10 天**（按 **10 天** 摊销 API，仍用同一日均 **8.30/天** 的 **全站** 费用，逻辑上只能做 **压力情景**，不能当成单用户成本）：

\[
8.30 \times 10 = 83\ \text{（与 }F_7\text{ 同币种·全站 10 天费用量级）}
\]

若 **83 为 USD**，则约 **598 CNY**，**远高于 ¥19.9**——说明 **全站一周期的 API 费不可能由单个冲刺用户承担**；必须通过 **多用户分摊 + 控制免费与泄漏** 才能成立。

### 0.6 结论（数字层面）

| 指标 | 数值（按你提供数据） |
|------|----------------------|
| 7 天 API 费用 | **58.09**（控制台币种） |
| 日均 | **~8.30** |
| 30 天线性外推 | **~249**（同币种） |
| 折合 CNY（API，7.2 汇率） | **~1793/月** |
| 粗算「每次请求」摊销 | **~0.011～0.012**（同币种） |
| 仅抵 API 的月度 VIP 数（29.9 CNY、零其它成本） | **~60** |
| 3/20 请求占比（8 天样本） | **~57%**，且成功率 **~35%**（需防无效成本） |

**下一步建议（把测算变成「可决策」）**：从控制台导出 **同币种** 的 **(1) 按日费用 3/15～3/23**、**(2) 按模型 token**、**(3) 付费用户人数与 ARPU**，即可计算 **真实毛利**，而不只是线性外推。

---

## 一、当前产品定价与权益（代码事实）

| 产品 | 价格（约） | 备注 |
|------|------------|------|
| **简历 PDF 导出**（编辑器「导出 PDF」） | **免费**（需登录） | `App.tsx`：`handleExportPDF` → `doExportPDF`，注释写明「导出不产生服务端成本，免费」。**无任何支付校验。** |
| 单次 · 面试记录保存 | ¥4.9 | `interview_export`（与 `authService` 首次免费等逻辑一致） |
| 冲刺 VIP | ¥19.9 / **10 天** | `vip_sprint` |
| 月度 VIP | ¥29.9 / **月** | `vip_monthly` |

**关于「¥4.9 简历下载」SKU（`resume_download`）**  
`paymentService` / `api/xorpay/*` / `DownloadPayModal` 仍保留 **¥4.9 单次下载** 商品与回调，但 **`setShowDownloadModal(true)` 在仓库中无调用**，付费弹窗**不会出现在主流程**。若产品已永久免费导出 PDF，可视作**遗留代码与配置**，后续可清理或仅保留历史订单兼容。

**免费用户（`api/gemini/proxy` 与 `supabaseClient` 一致）**

| 维度 | 额度 |
|------|------|
| 诊断（含与诊断同池的 `resume_edit`） | 共 **3 次** |
| 模拟面试 | **1 次** |
| 翻译 | **3 次** |
| 职业探索 | 日上限 + 免费用户 **3 格**「计费」体验池（成功才记） |

**VIP（服务端 `MEMBERSHIP_LIMITS`）**

| 维度 | 上限 |
|------|------|
| 诊断类（含 `resume_edit` / `auto_rewrite` 月统计） | **200 次 / 月**（展示上可称「无限」） |
| 模拟面试 | **300 次 / 月** |
| 翻译 | 代码中为 **-1**（与「无限」文案一致） |

**Pro / Special** 另有逻辑（Pro 无限制计次；Special 日总操作 **20**），此处不展开，定价讨论以主路径 **免费 + VIP** 为主。

---

## 二、API 调用结构（与成本直接相关）

### 1. 模型与入口

- 主路径大量使用 **`gemini-3.1-pro-preview`**（失败时可降级到 2.5-pro / flash 等，见 `geminiService.ts`、`interviewService.ts`、`careerService.ts`、`api/gemini/proxy` 白名单）。
- 所有生产请求经 **`/api/gemini/proxy`**，成本 = **Google Generative Language API 计费 × 实际 token**（另加 Vercel 执行时间、出站流量）。

### 2. 单次「简历诊断」大致消耗

- 主流程：`createAIClient('diagnosis')` 流式生成（长 prompt：简历 + JD + 指令）。
- 可能伴随：`auto_rewrite`（不计入诊断配额，但**仍走模型**）、`file_extract` / `transcribe`（辅助，**不占**试用配额但占模型与限流）。
- **量级**：通常 **1～N 次**流式会话（N 含自动重构、文件解析时更多），输入输出 token 与简历/JD 长度强相关。

### 3. 单次「模拟面试」消耗（成本大户）

- **全自动模式**（`interviewService`）：每轮 **2 次**流式（面试官 + 模拟候选人），默认 **`totalRounds: 8`**（`InterviewChat.tsx`），结束后 **1 次**总结流式。
- **粗算**：约 **8×2 + 1 = 17 次** `generateContentStream` / 会话；且上下文随轮次变长，**后几轮 token 明显高于前几轮**。
- **人机交互模式**：每轮用户回答后至少再调面试官，总次数与轮数、是否提前结束有关，仍属**多轮 × 长上下文**。

**计费口径**：`usage_logs` 里 **`action_type = 'interview'` 按「场次」计 1 次**，不是按 17 次 API 计；因此 **单次付费会员若高频跑满轮次，边际成本远高于「计一次」的直觉**。

### 4. 翻译 / 职业探索

- 翻译：`createAIClient('translation')`，一般 **单次调用**量级（仍可能很长文本）。
- 职业探索：`career_explore` + step（profile / directions / plan / jd_demo），每步 **`generateContent`**，免费用户有 **日上限 + 3 格体验池** 等逻辑。

### 5. 其它约束（影响成本上限）

- **限流**：例如每用户每分钟 **150 次**（`proxy.ts` 注释：8 轮面试约 17 次等），主要防滥用与 429，不是定价本身。

---

## 三、其它成本（建议你们用账单填数）

| 类别 | 说明 |
|------|------|
| **Google Gemini** | 按模型 × token；Pro/Preview 单价通常显著高于 Flash。**应占收入成本大头**。 |
| **Vercel** | Serverless 执行时间、带宽、（若）Pro 席位。 |
| **Supabase** | 数据库、存储、Auth、出站流量；用户量上来后从免费档上浮。 |
| **Upstash Redis** | 限流；用量一般小于前几项，但生产必需。 |
| **支付渠道** | 微信/支付宝等：**按笔手续费**（常见量级约千分之几到加一点固定费，以合同为准）。 |
| **人工与支持** | 退款、客诉、运营：建议按单或按用户摊销，否则低价高用量会亏。 |

---

## 四、测算框架（如何自己算「是否合理」）

### 1. 先固定「单位成本」假设（需替换为真实值）

设（示例，**非建议定价**）：

- \(C_{\text{diag}}\) = 单次完整诊断（含常见附加调用）平均 **美元成本**  
- \(C_{\text{int}}\) = 单次完整模拟面试（默认 8 轮 + 总结）平均 **美元成本**  
- \(C_{\text{tr}}\) = 单次翻译平均成本  

用 Cloud 控制台或导出账单，按 **模型 × token** 反推最近 30 天均值最稳。

### 2. 免费用户获客成本（CAC 中的「AI 部分」）

近似：

\[
\text{FreeAI}_\text{max} \approx 3 \cdot C_{\text{diag}} + 1 \cdot C_{\text{int}} + 3 \cdot C_{\text{tr}} + C_{\text{career}}
\]

\(C_{\text{career}}\) 与是否跑满职业探索、是否重复生成有关。  
若产品目标是 **用免费额度换转化**，需满足：**预期 LTV（终身付费）× 转化率** 能覆盖 \(\text{FreeAI}_\text{max}\) + 其它固定成本；否则要么**收紧免费**，要么**提高付费率/客单价**。

### 3. VIP 最坏情况（用于压力测试定价）

对「**真把额度跑满**」的极客用户：

\[
\text{VIPWorst}_\text{month} \approx 200 \cdot C_{\text{diag}} + 300 \cdot C_{\text{int}} + \infty \cdot C_{\text{tr}} \quad (\text{翻译在代码上为 -1})
\]

**注意**：现实中几乎无人每月 **300 场**完整 8 轮面试；该式给出的是**上界**。更合理的测算是：

- **P50 / P95 月用量**（每场面试轮数、每月诊断次数）× 单价。

若 **P95 用户**的月成本已接近或超过 **¥29.9**（再减支付费与税），则当前 VIP **从纯变量成本角度偏紧**。

### 4. 单次 ¥4.9（面试记录保存等；不含简历 PDF）

- **简历 PDF**：当前实现为 **客户端导出**（html2canvas + jsPDF），**不收费**；成本讨论里勿再把它算作 ¥4.9 收入项。  
- **面试记录保存 ¥4.9**（若仍启用）：若仅覆盖 **存储 + 带宽 + 少量逻辑**，定价空间通常仍可接受；**AI 成本**多在模拟面试流程中已发生，需与会员权益区分。

---

## 五、结论：当前定价「是否合理」（定性）

在**不掌握你们真实 token 账单**的前提下，只能给**结构判断**：

1. **单次模拟面试的 API 成本很可能远高于单次诊断**（轮次多、双角色、长上下文、再加总结）。**1 次免费面试 + 3 次诊断** 的免费组合，对获客是「大方」的，**获客成本可能主要压在这一项**。
2. **VIP「月 200 诊断 + 300 面试」**在代码上是**硬上限**（展示可称无限），若出现少量「跑满型」用户，**边际成本可能非常夸张**；是否健康取决于：**绝大多数用户远低于上限** + **高价模型是否混用 Flash 降本**（产品策略问题）。
3. **¥19.9 / 10 天 vs ¥29.9 / 30 天**：若日均用量接近，短期包可能 **ARPU 更高**；需看转化目标（冲量 vs 利润）。
4. **¥4.9 单次**：若不与额外 token 强绑定，通常合理空间较大；核心仍看 **Gemini 总账**。

**一句话**：定价是否合理 = **「真实 token 成本 + 支付与固定成本」相对「收入与转化」是否匹配**；代码里**权益偏慷慨的一侧是「面试场次 × 每场多轮调用」与「VIP 月限数值」**。

---

## 六、合理且「有边界」的定价逻辑（建议作为后续改价原则）

以下为**逻辑层**建议，便于以后只调参数、少改代码结构。

### 1. 成本锚点（Cost anchor）

- 以 **「单次完整模拟面试（默认轮数）」** 与 **「单次完整诊断」** 为两个核心成本锚点；其它功能（翻译、职业探索、导出）挂靠或折算为「等价诊断点」。
- 内部用 **「点」或「算力分」** 统一折算，对外仍用「次数/会员」表述。

### 2. 免费层（Free）

- **目标**：足够完成一次「啊哈时刻」，但不让羊毛党长期替代付费。  
- **边界**：  
  - 免费面试 **1 次** 是否保留：取决于 \(C_{\text{int}}\) 与转化率；若面试成本过高，可改为 **缩短默认轮数** 或 **仅开放「精简面试」**（需产品配合）。  
  - 诊断 **3 次**：相对可控；注意 **auto_rewrite / OCR** 不计次带来的成本泄漏（技术债，见 `REVIEW_ACTION_ITEMS.md`）。

### 3. 订阅层（VIP）

- **原则**：订阅价应覆盖 **「中高分位用户月用量」** 的 token 成本 + 支付 + 固定摊销，而不是覆盖「理论最大值」。  
- **边界**：  
  - **月上限（200/300）** 是**风控线**；若账单显示 P95 仍安全，可略放宽展示文案；若成本吃紧，应优先 **降上限** 或 **高用量用 Flash / 限制轮数**，而不是先涨价。  
  - **冲刺 10 天**：适合作为「大促/冲量」，定价要防止 **10 天内跑满多轮面试** 的成本高于收款。

### 4. 单次购买（¥4.9，以实际在售 SKU 为准）

- **原则**：与「已消耗的 AI」解耦；**简历 PDF** 当前为免费导出，不再用 ¥4.9 覆盖。仍上线的单次项（如 **面试记录保存**）定位为 **存储/权益解锁**。  
- **边界**：若未来对 PDF 再收费或增加「云端排版后再导出」等 **含新算力** 的步骤，应单独标价或纳入会员。  
- **代码债**：`resume_download` 若不再售卖，宜从文案与下单入口收敛，避免文档/配置与体验不一致。

### 5. 价格调整时的「安全顺序」（改动边界）

在不想大改架构的前提下，通常 **从低到高** 的侵入性为：

1. **调数字**：会员价、周期、免费次数、VIP 月上限（200/300）、默认面试轮数（8→6 等）。  
2. **调模型策略**：高用量场景默认 Flash、Pro 仅用于关键步骤（需体验评估）。  
3. **调计费口径**：例如面试按「轮」或「分钟」折算（需前后端一致，侵入性大）。  
4. **调产品线**：拆分「轻量面试」与「全真 8 轮」为不同 SKU。

---

## 七、建议补齐的数据（与「〇、实证测算」衔接）

| 数据项 | 用途 |
|--------|------|
| **同币种**月度账单 & **按模型**拆分 | 把 §0 的线性外推换成 **真实月成本** |
| 按日费用与 **按日请求** 对齐到同一时区 | 解释 **费用峰在 3/19、请求峰在 3/20** 是否 UTC 切日导致 |
| 平均每场面试 token（或费用）与轮数分布 | 校验面试是否成本中心 |
| MAU、付费人数、**ARPU** | 计算 **API+其它成本 / 付费用户** 是否 < 净收入 |
| 支付渠道实际费率 | 把 ¥29.9 换成 **净收入** 再算保本用户数 |
| 免费→付费转化率 | 判断免费额度是否过宽 |

---

## 八、与代码/待办的关系

- **成本泄漏**（`file_extract` 等不占配额、重复记账）会扭曲真实成本，定价讨论前应优先按 `REVIEW_ACTION_ITEMS.md` 理清 **计量准确性**。  
- 本文**不修改任何价格或配置**；达成一致后，可再单列「改价清单」（产品文案 + `paymentService` / `xorpay` / `xunhupay` / `MEMBERSHIP_LIMITS` 同步修改点）。

---

*文档版本：讨论稿 v2 — 已加入基于仪表盘的实证测算（§〇）。*
